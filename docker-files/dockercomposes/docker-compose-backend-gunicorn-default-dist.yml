version: '2.1'
services:
  backend:
    container_name: ${APP}-backend-default-dist
    build:
      context: ${APP_PATH}
      dockerfile: ${DF_FOLDER_SUBPATH}/Dockerfile.backend-gunicorn-default-dist
    restart: always
    ports:
      - "8081:8100"
