version: '2.1'
services:
  apiviz-back:
    container_name: ${APP}-prod
    build:
      context: ${APP_PATH}
      dockerfile: ${DF_FOLDER_SUBPATH}/Dockerfile.backend-gunicorn-prod
    restart: always
    ports:
      - "8100:8100"
