version: '2.1'
services:
  backend:
    container_name: ${APP}-backend-prod-dist
    build:
      context: ${APP_PATH}
      dockerfile: ${DF_FOLDER_SUBPATH}/Dockerfile.backend-gunicorn-prod-dist
    restart: always
    ports:
      - "8100:8100"
